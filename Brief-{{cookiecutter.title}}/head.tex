% Header File defining LaTeX Template

% Language and Utf-8 Support
\usepackage[utf8]{inputenc}
\usepackage{polyglossia}
\setmainlanguage{german}
\setotherlanguages{english}

%% Layout / Structure
%%

% Document Layout
\usepackage[inner=25mm, outer=20mm]{geometry} % Geometry according to DIN 5008

% Header and Footer
\usepackage{fancyhdr}
\pagestyle{fancy}
\fancyhf{} % Clean header and footer

% Package for tabularx Tables
\usepackage{tabularx}


%%	Typography / Fonts
%%

% Setting Template-Fonts
\usepackage{fontspec} 
\setmainfont{Linux Libertine}
\setsansfont{Optima}
\setmonofont[Scale=MatchLowercase]{Menlo}

% Typography Enhancements
\usepackage{microtype}

% Line spacing
\usepackage{setspace}

%%	Literature / Cites
%%

% Zitate
\usepackage[autostyle]{csquotes}

% Urls / Hyperlinks
\usepackage[colorlinks, linkcolor = black, citecolor = black, filecolor = black, urlcolor = blue]{hyperref} 

% Graphics
\usepackage{graphicx}

%% 	Own definitions / commands
%%

% Itemize* for compact Itemize Environment 
\newenvironment{itemize*}%
{\begin{itemize}%
	\setlength{\itemsep}{0.3em}%
	\setlength{\parskip}{0pt}}%
{\end{itemize}}

\renewcommand*{\raggedsignature}{\raggedright} % Stop the signature from indenting

% Koma vars for letter setup
\hypersetup{pdfpagemode=None}
\KOMAoptions{
        paper=a4,
        pagenumber=botcenter,
        fromalign=right,
        fromphone,
        fromemail,
        backaddress,
        foldmarks,
        enlargefirstpage
}